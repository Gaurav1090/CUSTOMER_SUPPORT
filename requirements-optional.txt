# Optional integrations. Install only when needed:
#   pip install -r requirements-optional.txt
redis
# langfuse moved to requirements.txt -- see the comment there. It's a hard
# requirement now, not optional.
ragas

# fsspec backends for the ingestion landing_path -- install only the one(s)
# matching your cloud, not needed for local development:
gcsfs   # gs://
s3fs    # s3://
adlfs   # abfs:// (Azure Data Lake)
