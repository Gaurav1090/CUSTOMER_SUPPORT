# Optional integrations. Install only when needed:
#   pip install -r requirements-optional.txt
redis
# Pinned >=3: utils/ops.py's build_langfuse_trace/finish_langfuse_trace use
# the OpenTelemetry-based span API (start_observation, span.score, ...)
# introduced in the v3 SDK rewrite. An unpinned install previously grabbed
# v4 silently -- the old code called the removed v2 `.trace()` method,
# which raised, got caught by a broad except, and made every Langfuse call
# a no-op with no error surfaced anywhere.
langfuse>=3,<5
ragas

# fsspec backends for the ingestion landing_path -- install only the one(s)
# matching your cloud, not needed for local development:
gcsfs   # gs://
s3fs    # s3://
adlfs   # abfs:// (Azure Data Lake)
