name: CD - Dev

# Deploys on every merge to develop -- requirement: "deployment should
# happen when code is merged on the developer branch."
on:
  push:
    branches: [develop]

jobs:
  tests:
    uses: ./.github/workflows/_reusable-tests.yml

  build:
    needs: tests
    uses: ./.github/workflows/_reusable-build.yml

  deploy:
    needs: build
    uses: ./.github/workflows/_reusable-deploy.yml
    with:
      environment: dev
      image: ${{ needs.build.outputs.image }}
