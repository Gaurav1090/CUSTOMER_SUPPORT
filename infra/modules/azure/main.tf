# Intentionally empty -- see README.md. This file exists so `terraform
# validate`/`terraform fmt` treat infra/modules/azure as a real module
# rather than a broken/missing one, and so the extension steps in
# README.md have a concrete file to add resources to.
